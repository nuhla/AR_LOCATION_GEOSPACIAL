using System.Collections;
using System.Collections.Generic;
using ARLocation.MapboxRoutes;
using TMPro;
using UnityEngine;

public class RoutError : MonoBehaviour
{
    public TMP_Text _Error;

    public MapboxRoute mapboxRout;
    private object mapbox;
    private string _mapboxError = null;

    // Start is called before the first frame update

   

}
